doctype html
html(lang='en')
    head
        meta(charset='utf-8')
        title (Categoy)
    body
        form(action = `/categories/${category.id}?_method=PUT` method = "POST")
            input(type="text" name="title" placeholder="Ingresa el titulo aqui" value=`${category.title}`)
            input(type="text" name="color" placeholder="ingresa el color" value=`${category.color}`)
            input(type = "submit" value = "guardar cambios" ) 